# Milestone 2

- Done:
  - text task router
  - approval classes
  - SQLite audit log
  - deterministic local tool execution for safe commands
  - Codex execution bridge
  - Gemini planning bridge
  - CLI commands for `task` and `audit`
  - resumable approval flow via `approve --latest`
  - voice-path routing into the orchestrator for non-builtin commands
  - local browser bootstrap with headless screenshots and browser-state reporting
  - authenticated localhost HTTP API with `/task`, `/approve`, `/audit`, and `/browser/state`
  - Telegram transport on top of the same orchestration core
  - local memory summaries and note storage
  - unit coverage for routing and audit

- Still open inside M2:
  - richer deterministic local tool surface
  - voice-native approval prompts
  - deeper browser automation on top of the same approval/audit core
  - Android / Tailscale clients on top of the same task API
