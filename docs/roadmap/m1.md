# Milestone 1

- Done:
  - repo bootstrap
  - service and CLI
  - Unix-socket control plane
  - armed listening
  - transcript-gated wake handling
  - optional `openWakeWord` backend interface
  - local ASR + TTS
  - deterministic transcript normalization for coding/math/shell-style phrases
  - barge-in interruption
  - backend benchmark command
  - fixture recording/eval commands
  - end-to-end pipeline replay evaluation
  - unit and integration tests

- Still open inside M1.1:
  - collect real fixture corpus
  - benchmark dedicated wake-word model on this machine
  - measure live `Dude, hello` end-to-end latency
  - benchmark recorded coding/math transcript quality on real fixtures
